# 主线场景接线维护记录

本次按审阅者确认的统一修复计划执行。源数据为 `data/scenes.json` 与 `data/events.json`，引擎为 `src/events.js`；回归见 `tools/test-main-route.mjs`。本文件描述当前行为与接线约定。

## 接线约定

1. **先切景，再播到达描写**：任何进入新场景的路线都必须先执行 `changeScene`，目的地描写（“一进入车厢……”“你踏进2号车厢。”）必须落在新背景上。
2. **出发文字留在出发场景**：推门、过门、起步这类句子属于出发场景，不因为下一句要切景而提前切。
3. **点击目标必须语义匹配**：场景热点的 `clickEvent` 指向的事件，其内容与状态修改必须属于该场景；门类热点优先复用 `E_GO_<出发>_<到达>` 命名。
4. **同一段落只播一次**：进车厢类检定用独立旗标闸门；到过一个场景的记录（如 `crew_met`）不因路线不同而缺失。
5. **进车先点门，剧情不许代劳**：从一节车厢进入下一节车厢必须由玩家点击门热点（`door_<出发>_<到达>`）触发；剧情事件不得用 `changeScene` 直接替玩家完成跨车厢。否则玩家没有机会在门前做选择——3号→2号的门同时是里世界入口，一旦由剧情自动进车，一条主要支线就会被整段跳过。

## 修复对照（第一轮：统一接线）

| # | 原现象 | 现在的行为 |
| --- | --- | --- |
| 1 | 5号右门 `door_05_to_04` 直连 `E_013`，无切景、无到达描写，直接弹医学检定 | 右门改指新增 `E_GO_05_04`（`changeScene carriage_04` + 置 `crew_met` +「你穿过门，来到4号车厢。」），**不触发检定** |
| 2 | 剧情路线（`E_010_F`／`E_010_JOIN`／`E_011_S`／`E_011_F`／`E_012_AFTER`）直接落进只有检定动作的 `E_013` | 五条路线统一 `next → E_013_ENTRY`（`changeScene carriage_04` +「一进入车厢，你就发现一名重伤昏迷的乘务员倒在地上。」），再由 `E_013` 检定 |
| 3 | `E_012_AFTER` 在5号背景上说「你来到4号车厢。」 | 该句收缩为「本能驱使着你向前跑去。」，到达描写归 `E_013_ENTRY` |
| 4 | `E_013` 每次进入4号都会重播检定（6号↔7号门往返会重走 `E_010_F`） | `E_013` 首动作改为 `conditionalJump`：`crew_04_entry_medical_done` 为真时落到静默落点 `E_013_REVISIT`（`actions: []`）；检定与 `crew_met` 只在首次置位 |
| 5 | 进车厢检定失败不记录结果，后续 `E_020` 会落到「她已经醒了」的分支 | `E_013_F` 补 `setFlag crew_04_medical_failed = true`，与 `E_014_F` 一致；`E_013_S` 补 `crew_04_interacted = true`（已交谈后倒地乘务员不再可点） |
| 6 | `E_DOOR_04`（4号→3号）在行李已清理后静默切景 | 末尾补「你穿过门，返回3号车厢。」，与 `E_GO_02_03` 同形 |
| 7 | 3号→2号（`E_023_LOOP`→`E_024`→`E_025`→`E_026`）全程停在3号背景 | `E_024_S_CONTINUE` 首部、`E_024_F` 末尾各补 `changeScene carriage_02`；`E_025` 收敛点因此场景中立（**第二轮已作废**：改为由玩家点门进车，`E_024` 随之退出主线，见下） |
| 8 | `clicker_02` 指向 `E_021`（内容是「回到3号车厢，你取出工具。」） | 改指 `E_026`，直接进入2号车厢的怪物遭遇 |
| 9 | `control_27`（前进与停车把手）指向 `E_027`（2号车厢潜行检定） | 改指 `E_032`（打开操作面板） |
| 10 | `E_027_S` 潜行通过不置 `carriage_02_passed`，前门此后永远只能「强行推门」 | `E_027_S` 补置 `carriage_02_passed = true` |
| 11 | `E_GO_02_FRONT_DOOR` 的「推开安全门」指向 `E_026`（怪物遭遇） | 改指 `E_031`（到达先头车厢） |

新增内容：事件 `E_GO_05_04`、`E_013_REVISIT`；旗标 `crew_04_entry_medical_done`；回归脚本 `tools/test-main-route.mjs`。存档版本（3）与数据格式版本（3）不变；新旗标缺省视为未触发。旧档若已进过4号但无 `crew_04_entry_medical_done`，下次经剧情路线进4号会再播一次进车厢检定，属一次性的可感差异，不产生死路。

## 修复对照（第二轮：2号车厢改由玩家点门进入）

审阅者反馈：3号→2号的剧情链会自动把玩家带进2号车厢并一路推进，玩家点不到那扇门，里世界支线形同虚设。

| # | 原现象 | 现在的行为 |
| --- | --- | --- |
| 12 | 剧情链 `E_022_ITEM → E_023 → E_023_LOOP → E_024` 全自动串联，最后 `E_024_S_CONTINUE`／`E_024_F` 各自 `changeScene carriage_02` 并接 `E_025 → E_026`，把玩家直接送进2号 | `E_022_ITEM` 拿完手电即停（不再自动接 `E_023`），玩家留在3号车厢自己决定走哪扇门；进2号只能点 `door_03_to_02`（`E_501`），首次进里世界、已进过则走主线 `E_DOOR_03`（由 `845babf` 落地） |
| 13 | 进2号只剩自动路径，`E_025`（进入2号·听到喘息，含乘务员同行分支 `E_025_CARRIED`）成为主线进车描写，里世界出口无人接管 | `E_025` 改由里世界返程承接：`E_524` 回到真2号后经 `E_524_DONE`（乘务员同行时先经 `E_524_CREW`）进入 `E_025`；`E_025` 与 `E_025_CARRIED` 的 `next` 均已删除，喘息段播完停下，玩家照明后自己点 `clicker_02` 触发 `E_026` |
| 14 | `E_524` 与 `E_025` 各写了一遍喘息（「你听见明显的喘息声。」「那不是人类的喘息。」） | 喘息描写只在 `E_025` 保留一次；`E_524` 的到达句收缩为「这一次，门后是真正的2号车厢。」 |

本轮无新增事件、无新增旗标；存档版本（3）与数据格式版本（3）不变。已进过里世界但在旧档里停在2号的存档，下次离开里世界或点门时按新规则继续，不产生死路。

## 验证

`npm run check` 包含既有三组回归与 `tools/test-main-route.mjs`。主线回归同时锁定结构接线（热点目标、`next`、闸门、条件）与逐句场景轨迹：右门不检定、剧情路线先切景后播到达描写、进车厢检定只发生一次、4号折返有描写、`E_022_ITEM` 与 `E_023_LOOP` 守住「剧情不自动进车」、里世界返程 `E_524` 先切真2号再播到达描写并接 `E_025`（播完不自动进 `E_026`）、Clicker 与控制杆不再跑到别的事件。里世界回归（`tools/test-inner-world.mjs`）另锁定：`door_03_to_02` 仍指 `E_501` 且保留「到过伪4后走 `E_DOOR_03`」的守卫、返程链路必须经过 `E_025`、喘息描写在返程只播一次。

浏览器验收路径：5号右门（只过门）→ 4号；另一局读报或侦查失败后自动进4号并检定一次；左门回3号有折返描写；3号取钥匙与手电筒后剧情停下（停在3号、门可点）→ 点 3号→2号 的门进里世界 → 逐门返程回真2号 → 播喘息段后自行照明、点 Clicker；潜行通过到先头车厢，再回2号点前门直接进先头车厢；点控制把手打开操作面板。另跑一次「已进过里世界后回3号点门」直接进2号（`E_DOOR_03`）。

## 已发现但本轮不改

1. `ev510_flower_sea` 只写不读：`E_510` 置位，`E_029` 的花海→Trauma 分岔已在 `541aad3` 删除并被 `tools/test-inner-world.mjs` 锁定，`E_515`（trauma 结束页）成为死事件；但 `docs/inner-world-presentation.md` 仍写「Trauma 仍由涉足花海后到达原真结局触发」，数据与文档互相矛盾，需要内容侧决定恢复分支还是改文档。
2. 6号↔7号门可反复往返：`E_905 → E_009` 全段（含 `ev010_san_01` 检定）会重播，可重复扣 SAN；`E_009` 已置 `ev009_seen` 但无人读取，说明原设计是只播一次，建议下一轮加该守卫。
3. 6号右门 `door_06_to_05` 无门槛，可跳过便签/地图/7号直接进5号（连带跳过 `E_010`／`E_011` 与报纸线索）。
4. 死事件清单（无引用或只被死事件引用，即不可达）：`E_010_JOIN`、`E_018_CLEAR_PATH`、`E_02_DECIDE`、`E_022_S`、`E_022_F`、`E_023`、`E_023_PHONE`、`E_023_LOOP`、`E_023_AGILITY_SUCCESS`、`E_023_THROW_AFTER_AGILITY_FAIL`、`E_023_THROW_AFTER_AGILITY_FAIL_FAIL`、`E_023_BOTTLE`、`E_023_CHOICE`、`E_024`、`E_024_S`、`E_024_S_KNOWLEDGE`、`E_024_S_CONTINUE`、`E_024_F`、`E_025_SINGLE`、`E_025_MULTI`、`E_026_FRONT_LEGACY`、`E_030_TUG`、`E_033_S`、`E_033_F`、`E_515`。本轮只修可达路径接线，不做删除，便于审查。
5. `README.md` 的「当前开发进度」关于 `conductor_tug` 的触发点（写 `E_026`、成功接 `E_029`、失败进 `E_030_TUG`）与实际（`E_033` 触发，结算动作跳 `E_034`／`E_035`）不符。
6. 潜行通过后不置 `clicker_cleared`，回2号可重复点 Clicker 触发遭遇；按模组「你还得从它旁边过」视为设计内。
7. `crew_04_medical_success`、`crew_04_dead` 只写不读（未使用的派生旗标）。
8. 「认知崩塌」（`E_023`／`E_023_PHONE`／`E_023_LOOP`）与「查看2号车厢·光源侦查」（`E_024` 全段，含 `ev024_light_01`）目前不可达：`845babf` 把 `E_022_ITEM` 断在拿完手电处、又把 `E_023_LOOP` 末尾接去里世界入口 `E_501`，但没有任何热点触发 `E_023`，所以这段演出（车厢编号变成3、灯灭了、广播「请不要下车」）目前一局都不会播。内容侧需要决定是接回某个触发点（并加一次性守卫，避免重复点门重播）还是另写替代演出。
9. 3号→4号的门（`door_03_to_04 → E_GO_03_04`）在剧情停下后仍可点，会说「你穿过门，返回4号车厢。」；等第 8 项把认知崩塌接回可达路径后，「来路的门已经消失」的描写会与它还开着的事实冲突，需要一并处理。
