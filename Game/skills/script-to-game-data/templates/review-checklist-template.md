# 剧本转换审查清单 · {{YYYY-MM-DD HH:mm}}（{{事件范围，如 E-001~E-006}} 批）

## 先读这里（约 2 分钟上手，请务必读完）

剧本「{{标题}}」已清洗成 {{N}} 个候选事件（**尚未写入 `data/`**）。转换中凡需要你**拍板**的地方都列在下面，分两类：

- **一、请你判断的 N 件事**：{{N}} 条提问。多数是"剧本里某处我这样理解 / 这样处理了，对吗？"，附有我的建议，你只需逐条给答复。
- **二、素材指定区**：{{M}} 行。本批候选要在游戏里正常运行，依赖这些素材文件（场景背景 / 物件贴图 / 物品图标等）。**每行必须给出显式结果**（沿用 / 新建 / 委托占位补位 / 暂缓并注明影响），留空则本闸门不通过。

怎么作答：

1. 每条提问勾选「我的答复」：**同意**（按建议处理）／**需要调整**（请在「说明」里写清替代方案）／**本次跳过**（本条先不做；建议在「说明」写清去向，如"交给框架组""随下批"）。
2. 每行素材在「你的指定」勾选并补全：**沿用**已有文件／**新建**（给出文件名与内容描述）／**本行暂缓**（注明影响——依赖它的候选将不落地）。
3. 想补充任何说明，写在第 3 节「补充说明」，用 `#Q1`、`#M1` 引用条目即可。
4. 全部处理完，在文末「收尾」按固定口令回复 agent 即可。

> agent 用来定位的技术细节（类别、内部命名、落点文件等）收在文末「附录 · 执行台账」里，**仅供 agent 使用，你不需要阅读**。

## 头部（agent 生成时填写）

- ★ 剧本：{{剧本标题或文件名}}
- ★ 输入文件：{{路径；多个用“；”}}
- 生成时间：{{YYYY-MM-DD HH:mm}} ｜ skill 版本：{{v1}} ｜ 产出：候选清单与候选 JSON（**未写 `data/`**）
- ★ 事件范围：{{如 E-001~E-006}} ｜ 本批转换的剧本事件编号
- 隔离编译验证：{{通过（N 个候选事件并入临时副本，退出码 0）/ 未通过：原因}}
- 本批候选：{{共 N 个候选事件}}；{{与既有 data 数据的关系，如：仅新增本批命名空间，既有条目零改动}}

## 一、请你判断的 {{N}} 件事

> 每条只问一件事。勾选「我的答复」，需要调整 / 跳过时请在「说明」里写清。
> 不必看懂内部实现——看不懂的措辞请直接要求我在「说明」里换种说法解释。

### Q1 {{人话标题，如：事件 E-005 灵感失败时，E-006 的 SAN 损失要更重吗？}}

- 剧本原文：{{事件 E-0xx 中的原句短引，加引号}}
- 我的处理：{{一句人话说明转换器做了什么；不要出现内部命名/行话；必要时括号注明对应剧本位置}}
- 请你判断：{{实际需要拍板的问题}}
- 建议：{{默认方案，一句话}}
- 若按建议：{{效果}}
- 若本次跳过：{{影响——哪些内容将保持“仅候选、不落地”}}
- 我的答复：
  - [ ] 同意
  - [ ] 需要调整
  - [ ] 本次跳过
- 说明（需要调整时写替代方案；跳过时写去向）：

### Q2 {{人话标题}}

- 剧本原文：{{…}}
- 我的处理：{{…}}
- 请你判断：{{…}}
- 建议：{{…}}
- 若按建议：{{…}}
- 若本次跳过：{{…}}
- 我的答复：
  - [ ] 同意
  - [ ] 需要调整
  - [ ] 本次跳过
- 说明（…）：

（agent：逐条复制上方模板；**一条只承载一个决策点**，禁止把多件事塞进同一条。）

## 二、素材指定（每行必须有显式结果，否则闸门 1 不过）

> 含义：下面每行是候选 JSON 能正常运行所依赖的素材。**已存在**的素材只需确认沿用；**缺失 / 推断占位**的必须补齐指定：新建（你给文件名与内容描述）、**委托占位补位**（由 agent 生成简单占位 SVG 并自行命名，见「你的指定」说明），或显式选择"本行暂缓"并注明影响。留空的素材行 = 闸门 1 未通过，agent 会停下追问。
> 说明：agent 在盘点时已检索过仓库根源目录 `Assets/`（只按文件名判断，未查看图片内容）；命中的图片已复制到运行目录 `assets/` 并自行命名，对应行状态标为「已存在（复制自 …）」，你可直接沿用或改指其它。**源目录 `Assets/` 始终保持只读，不会被改动。**

### M1 {{素材角色名，如：7 号车厢背景图}}

- 依赖：{{哪个场景/哪些候选事件需要它（人话）；必要时附内部引用：changeScene → {{scene id}} 等}}
- 所需形态：{{素材类型：场景背景图 / 物件贴图 / 物品图标}}；{{内容要点、画面风格参考（如现有 assets/xxx.svg）、其他约定}}
- 状态：
  - [ ] 已存在（{{assets/xxx.svg}}）
  - [ ] 缺失
  - [ ] 推断占位（转换时我假设：{{假设内容}}）
- 你的指定：
  - [ ] 沿用（{{assets/xxx.svg}}）
  - [ ] 新建：文件名 {{…}}、内容/风格 {{…}}（由你定名与内容，agent 只登记引用）
  - [ ] 委托占位补位：由 agent 生成**简单占位 SVG** 并自行命名（落在 {{assets/}} 下；仅纯色/几何示意，正式素材日后替换）
  - [ ] 本行暂缓，影响 = {{依赖它的哪些候选不落地}}
- 备注：

### M2 {{素材角色名}}

- 依赖：{{…}}
- 所需形态：{{…}}
- 状态：{{…}}
- 你的指定：{{…}}
- 备注：

（agent：逐条复制上方模板。若本批候选不依赖任何素材，在此注明"本批候选无素材依赖"即可，由用户确认后放行。）

## 补充说明

（你可以在这一节自由书写任何说明，引用条目用 `#Q1`、`#M1` 格式。）

## 收尾

全部完成（每条提问都有答复、每行素材都有显式结果）后，回复 agent 下列任一固定口令：

- **「已修改，继续」** → agent 按你的答复执行（改候选、记录状态），完成后再次停下汇报；
- **「按清单落地」** → 直接进入落地环节（闸门 2：先展示逐文件改动方案供你确认）；
- **「先修订候选 / 清单」** → 你已指出要改，agent 修订后重新停在本闸门；
- **「先不落地」** → 本次到此为止：仓库只留清单与候选，`data/` 零改动。

> 提示：以上口令与每题勾选项（同意 / 需要调整 / 本次跳过、沿用 / 新建 / 暂缓）是 agent 唯一认读的固定词；其余文字一律视为你的说明，不会被误解为指令。中途想暂停，直接说"先等等"即可，agent 会就地停止。

---

## 附录 · 执行台账（agent 内部用；审阅者可完全忽略）

> 仅供执行 agent 定位、执行与留痕。审阅者在正文作答即可，本段无需阅读或修改。
> 类别标记：A 自动推断｜C 检定规则｜D 数值物品｜E 素材场景｜F 框架缺口｜G 文本问题｜H 触发拓扑｜O 其他。
> 状态：待执行 →（agent 按答复）已同意 / 需修订 / 跳过。

| 编号 | 剧本位置 | 类别 | 内部引用（候选事件/命名/接线等） | 落地动作 | 落点文件 | 用户答复 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Q1 | {{如 E-005→E-006}} | {{A}} | {{如：E-006 拆 E_006A/E_006B；E_005_S→E_006A…}} | {{如：拆分 + 接线 + SAN 落地}} | {{data/events.json}} | {{agent 重读后填}} | {{待执行}} |
| Q2 | {{…}} | {{…}} | {{…}} | {{…}} | {{…}} | | |
| M1 | {{如：E-006 所在场景}} | {{E}} | {{如：changeScene → carriage_07；scenes.json background}} | {{background 沿用/替换/新建素材登记}} | {{data/scenes.json}} | | |
| M2 | {{…}} | {{…}} | {{…}} | {{…}} | {{…}} | | |
