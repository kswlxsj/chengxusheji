# 里世界演出维护记录

本次按审阅者确认的统一修复计划执行。源数据为 `data/events.json`，引擎为 `src/events.js`；原转换审查答复保留历史，本文件描述当前行为。

## 路线与切景

| 入口/操作 | 当前行为 |
| --- | --- |
| E_501（3号推门） | 3号车门 `door_03_to_02` → `E_501`：在3号播推门，素材就绪后切空车厢；**踏入那一刻即置 `carriage_06_eaten`**。门禁只认“到过伪4”：未到伪4时回3号仍可再进，到过之后推门直接走主线 `E_DOOR_03`。二次进入只播「你深吸一口气，伸手推开了通往2号的车门。」，跳过入口描写。**这扇门是进入2号车厢的唯一入口**：主线拿完手电筒后剧情停下（`E_022_ITEM` 不再自动接 `E_023`），玩家留在3号车厢自己点门；「认知崩塌」`E_023` 与「光源侦查」`E_024` 目前不在可达路径上（见 [主线接线维护记录](main-route-wiring.md) 未修项 8）。 |
| 花草右门 E_505 | 切伪4号并置 `inner_world_entered`（正式进过里世界，门禁从此刻锁死）。进场描写由 `E_506` 承担，只播一次（见下「描写去重」）。 |
| 空车厢左门 | 首次点击做一次静默判定：10%回6号（`E_525`，不自动调查便签）、60%门被关死、30%回3号（`E_502_CARRIAGE03`）。判定前先置 `ev502_return_rolled`，三条出口都算用掉这一次机会；此后重复调查一律落到 `E_502_LOCKED`，只播「门被关死，打不开。」并留在空车厢。到过伪4（`inner_world_entered`）后该门解锁，改为磨损门描写 `E_523` → `E_524` → 真实2号车厢。 |
| 花草左门 | 不设防：无论正程/回程都是「你向来路折返。」→ 切回空车厢（`E_503_BACK` → `E_502`），不再有 50/50 静默随机。 |
| 伪4号左门 | 一律回到花草车厢（`E_522`）。 |
| 花海调头 E_513 | 先播离开，切到假3号，再播放车厢变色与乘务员断头的五句入场段落；结束后由假3号左右门继续探索。 |
| 伪4号窗边交谈 E_516→E_520 | 有钥匙且选择交出时仍回到自由探索：点左门＝回花草车厢（`E_522`），点右门进花海。选择“留着”则改走拒绝钥匙演出与限时逃跑分支。 |
| 拒绝钥匙 E_519_KEEP | 三句乘务员对白后依次播放两张全屏 CG；两句威胁均为屏幕中央直出字幕，不使用对话框。随后显示 PC 惊恐对白与5秒限时选项；暂停冻结计时，超时默认“往左跑”。 |
| 限时逃跑·左 | 进入假2号车厢，四个血手印以 1.2 秒间隔在不同位置依次叠加，每次拍击各响一次。动画未结束时点左门只播三句打不开的惊恐对白；第四次拍击完成后，左门解锁进假1号，右门进假3号。 |
| 假车厢拓扑 | 假2左→假1；假2右→假3，并在 `inner03.JPG` 背景上播放车厢变色与乘务员断头段落；假3左→假2；假3右→既有花海。假1点击任一端车门都会播放扭曲、干呕与窗外乘务员段落，渐黑后切回真实3号车厢。 |
| 限时逃跑·右 | 直接进入既有花海事件 `E_510`。 |
| 返程顺序 | 伪4号左门 → 花草车厢 → 花草左门 → 空车厢 → 空车厢左门（磨损门）→ 真实2号车厢（`E_524`，钥匙按需归还）→ `E_025` 喘息段（含乘务员同行的低语分支，播完停下）→ 玩家自行照明后点 `clicker_02` 进 `E_026`。 |

描写去重：`E_501` 入口描写、`E_502` 空车厢描写、`E_503` 花草车厢初见描写、`E_522` 花草车厢返程描写、`E_523` 磨损门描写与伪4号进场描写 `E_506` 各自只播一次，分别由 `ev502_intro_seen` / `ev503_intro_seen` / `ev522_intro_seen` / `ev523_seen` / `ev506_intro_seen` 记录。假3号的断头段落由 `E_FAKE03_INTRO` 统一承接，从假2右门或花海调头抵达时都会播放。瓶子相关文字按约定不去重。

不再有回程旗标：原 `ev_inner_backtrack` 已删除；`E_513` 现在固定进入假3号，不再使用旧的 `ev513_intro_seen` / `E_513_REVISIT`。

`carriage_06_eaten` 的唯一来源是 E_501 踏入空车厢那一刻（每条进入里世界的路径都必经此处）；返回分支不再参与置位。6号场景的背景变体读这个旗标，指向 `assets/carriage-06-eaten.png`（被啃食·红色），且排在“便签消失版”之前，因此两个条件同时成立时显示被啃食画面。

伪4号背景默认完整雾景，点击窗外后在 `E_517` 的“雾气散开”后设置 `ev517_flower_revealed` 并刷新完整花海背景；重复调查直接独白。该标记与 `ev510_flower_sea` 污染独立。窗户蒙版跟随揭示标记，门的层级高于窗户以避免重叠误触。

## 演出与状态

- 乘务员生死按 `crew_04_dead` 分支（唯一置位点＝主线 `E_020_SECOND_MEDICAL_F`：3号车厢黑包流程里第二次医学检定失败；里世界只读，不置位）：死亡线 `E_506 → E_507`（疯狂低语 + 「停下来」惊吓），在世线 `E_506 → E_508`；窗边插话 `E_516` 在死亡时走 `E_516_DEAD`（「她不是已经死了吗？是谁在说话……？」）。`crew_met` 只表示「到过4号车厢」，在里世界的任何路径上都已为 true，不能用来判生死。
- 音效制作说明、状态标签、条件指令和数值待定提示不再显示给玩家；已有剧情声音描写保留。
- `innerWhisperScare`（触发点只有 `E_507`，即乘务员死亡线）：红底黑字“停下来”、600字/秒、剧烈震动、5秒有效播放时间，不加音效、不扣SAN。暂停冻结，点击/快进不缩短，取消清理覆盖层。覆盖层是 `pointer-events: auto`，会一并挡住 HUD，5秒内只能按 Esc 暂停（审阅者确认保留的行为）。
- `ev519_key_given`：钥匙尚未归还；交出时 `removeItem`，出口 `addItem` 后清除。`ev519_key_ever_given`：永久历史，用于控制杆抓握区20%→15%；不改变20秒时限和远端新增失败路线 `E_030_TUG`。
- 拒绝钥匙新增 `ev519_escape_left`（本次限时选择）、`ev_fake02_blood_1..4`（四层血手印）与 `ev_fake02_handprints_done`（拍击已完成）。旧档缺省均视为 false；假2号动画完成后重复进入不会重播拍击。
- 终局接线补齐：主游戏加载 `conductor-tug.js`，`E_028` 从旧检定改为实际争夺小游戏；自然失败或退出均经 `E_030_TUG` 收尾。四张正式素材复制到 `assets/ui/conductor-tug/`，独立预览仍可使用源素材。
- `lost` / `trauma` 接入结束页；Trauma仍是涉足花海后的原真结局替代。原真结局、失败结局保留已有结尾视频。
- 存档版本保持3。缺省新旗标视为未触发；`ev_fake01_crew_seen` 只控制假1号耳鸣后的背景切换，每次从假2进入假1都会先重置。旧档中的 `ev513_intro_seen` 不再产生效果。

## 明确暂缓

1. TODO：恢复鬼打墙演出；回程现在由「花草车厢 → 空车厢 → 磨损门」逐门承接，仍无鬼打墙旁白（原 `E_521` 占位已随窗边弹窗一并删除）。
2. 磨损车门缺素材：保留剧情描写，暂用空车厢左门的隐形热点承担（该热点的悬停/无障碍名仍写作「来路的车门」）。

第2项是审阅者明确接受的文本/画面例外。6号被啃食已落地：`Assets/Image/Scene/Background/里6.png` 复制为 `assets/carriage-06-eaten.png` 并接入 `carriage_06.backgroundVariants`。Assets 源素材不修改；已复制完整伪4号雾景到游戏运行资产。

## 验证

`npm test` 包含现有运行时测试和 `tools/test-inner-world.mjs`：逐句场景轨迹、门禁两拍、空车厢左门随机、各段描写去重、返程逐门链路、交互/侦察、手动拾瓶、钥匙交出/归还、拒绝钥匙演出、四次血手印/音效、假车厢拓扑、各类结局、暂停中等待切景、慢加载取消与加载失败回滚；并断言返程必经 `E_025` 且不自动进 `E_026`。

浏览器验收另检查真实素材、门窗交叠、5秒演出及暂停取消、背包显示和终局抓握区；本次新增的“进里世界后回6号显示被啃食画面（与便签消失版同时成立时啃食优先）”同样需要在浏览器里确认。接口变更详见 [接口说明](API使用说明.md)。

本次改造后的浏览器验收路径：3号取钥匙与手电筒（剧情停在3号车厢、门可点）→ 点 3号→2号 的门 → 空车厢（初见描写）→ 花草车厢 → 伪4号。交钥匙后仍按原返程链回真实2号；不交钥匙则分别验证：右跑直接进入花海，左跑进入假2号并在第四次拍击前点左门看到三句锁门对白，播放完成后走假2左→假1→黑场→真3，以及假2右→假3→假2/花海两条门路。另跑空车厢左门的随机出口、乘务员死亡/在世分支及伪4进场描写去重。

上一轮浏览器已验证：真实点击空车厢→花草拾瓶→伪4雾景→窗边揭示→交钥匙→花海调头→逐门返回2号；每句开始时实际 DOM 图片均已就绪。另验证惊吓暂停冻结/取消清理、20%与15%抓握区、迷失/Trauma结束页及存档恢复。浏览器初次发现的 DOM 图片就绪延迟已通过 `whenReady()` 等待修复。

最终计时实测：不暂停约5080ms，暂停约900ms后总时长约5996ms；原真结局与小游戏退出后的失败结局均继续进入原结尾视频。`npm run check` 包含编译、语法检查和各组回归；测试输出中的故意错误用于验证失败回滚，不代表测试失败。
