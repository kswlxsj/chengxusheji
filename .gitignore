# =====================================================================
#  .gitignore —— 让 Git 自动忽略「不该提交」的文件
# =====================================================================
#  写给不太熟悉 Git 的组员：
#    • 写进本文件的路径不会被 Git 跟踪，也不会出现在「更改/变更」列表里。
#    • 本文件修改后要正常提交（`git add .gitignore` + commit），规则才
#      会同步给全组。
#    • 已经被提交过的文件，即使写进这里也不会自动消失；需要先执行
#      `git rm --cached <文件名>`（只从仓库移除、不删你磁盘上的文件），
#      再提交一次移除操作。详见文件末尾「常见疑问」。
# =====================================================================

# ---------------------------------------------------------------------
# 一、Node.js / npm 相关（组里有人跑过 npm install 就会出现）
# ---------------------------------------------------------------------
# node_modules 是第三方依赖包在本机的安装目录，体积巨大，
# 且随时可以用 `npm install` 重新生成，绝不能提交进仓库。
node_modules/

# npm / yarn / pnpm 的运行日志
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------------------------------------------------------------------
# 二、操作系统自动生成的垃圾文件（与项目无关）
# ---------------------------------------------------------------------
# Windows：资源管理器生成的缩略图缓存
Thumbs.db
ehthumbs.db

# Windows：文件夹图标/背景等自定义配置（每人本机都不一样）
Desktop.ini

# macOS：访达自动生成（组里有人用 Mac 时很重要）
.DS_Store

# ---------------------------------------------------------------------
# 三、编辑器 / IDE 个人本地文件
# ---------------------------------------------------------------------
# 注意：我们【不】忽略 .vscode/settings.json —— 里面保存了 JSON Schema
# 校验配置，需要全组共享并一起提交，所以下面的规则只针对纯个人文件。
.vscode/*.log
# *.code-workspace 记录「你本机打开过哪些文件夹」，属个人文件
*.code-workspace

# ---------------------------------------------------------------------
# 四、各类临时文件与日志
# ---------------------------------------------------------------------
*.log
*.tmp
*~
*.bak

# =====================================================================
# 常见疑问
# =====================================================================
# Q：我把文件写进 .gitignore 了，但它还是出现在「更改」列表里？
# A：说明它以前被提交过。请运行：
#       git rm --cached <文件名>
#     （注意 --cached 只从仓库移除，不会删除你磁盘上的原文件）
#    然后像平时一样提交这次「移除」，之后该文件就会被忽略规则接管。
