# =====================================================================
#  .gitattributes —— 告诉 Git 每个文件「是文本还是二进制、换行怎么算」
# =====================================================================
#  写给不太熟悉 Git 的组员：
#    • 文本文件（代码、JSON、Markdown）：Git 按行对比改动。只要大家
#      换行符一致，就不会出现「我只改了一行却整份文件标红」的假改动。
#    • 二进制文件（图片、视频、Word、PDF）：无法按行对比，Git 会直接
#      跳过 diff 与自动合并，避免合并时损坏文件。
#    • 本文件修改后要提交，规则才会同步给全组生效。
# =====================================================================

# ---------------------------------------------------------------------
# 一、总开关：文本文件入库时统一换行符
# ---------------------------------------------------------------------
# text=auto 表示：提交进仓库的文本统一存成 LF；在 Windows 上检出时
# 自动还原成 CRLF。无论组员用什么系统，都不会产生整文件假改动。
* text=auto

# ---------------------------------------------------------------------
# 二、明确标记为「文本」的常见扩展名（可正常查看逐行改动）
# ---------------------------------------------------------------------
*.js     text
*.mjs    text
*.json   text
*.html   text
*.css    text
*.md     text
# SVG 本质是文本（XML），保留文本模式才能正常查看改动
*.svg    text
*.txt    text
*.cmd    text
*.ps1    text

# ---------------------------------------------------------------------
# 三、二进制文件：Git 不做 diff、不自动合并、不转换换行符
# ---------------------------------------------------------------------
# 图片（美术素材集中在这里：Assets/ 与 Game/assets/）
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.webp   binary
*.bmp    binary
*.ico    binary
# Photoshop / Aseprite 源文件
*.psd    binary
*.aseprite binary

# 音频 / 视频
*.wav    binary
*.mp3    binary
*.ogg    binary
*.flac   binary
*.mov    binary
*.mp4    binary
*.webm   binary

# Office 文档与 PDF（策划案、剧本、会议记录等）
*.doc    binary
*.docx   binary
*.xls    binary
*.xlsx   binary
*.ppt    binary
*.pptx   binary
*.pdf    binary

# 字体文件
*.ttf    binary
*.otf    binary

# 压缩包
*.zip    binary
*.rar    binary
*.7z     binary
