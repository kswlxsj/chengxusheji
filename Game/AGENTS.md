本项目受 Git 管理。独立发布时当前目录就是仓库根；在原始课程总仓库中，仓库根位于 `../`。执行 Git 命令前先用 `git rev-parse --show-toplevel` 确认当前布局，不要依赖固定的上级路径。

每次修改前先确认远端情况，留意 main 分支有没有新的提交；如果有，先更新到新的提交。
然后确认工作区情况,默认保留用户未提交的更改不动.
每次修改完后,使用简体中文进行一次或多次**清晰,明确,详细,符合社区规范的**git原子提交,包含你做的全部修改.
