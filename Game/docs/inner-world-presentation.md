# 里世界演出维护记录

本次按审阅者确认的统一修复计划执行。源数据为 `data/events.json`，引擎为 `src/events.js`；原转换审查答复保留历史，本文件描述当前行为。

## 路线与切景

| 入口/操作 | 当前行为 |
| --- | --- |
| E_501（3号推门） | 3号车门 `door_03_to_02` → `E_501`：在3号播推门，素材就绪后切空车厢；**踏入那一刻即置 `carriage_06_eaten`**。门禁只认“到过伪4”：未到伪4时回3号仍可再进，到过之后推门直接走主线 `E_DOOR_03`。二次进入只播「你深吸一口气，伸手推开了通往2号的车门。」，跳过入口描写。 |
| 花草右门 E_505 | 切伪4号并置 `inner_world_entered`（正式进过里世界，门禁从此刻锁死）。 |
| 空车厢左门 | 首次点击做一次静默判定：10%回6号（`E_525`，不自动调查便签）、60%门被关死、30%回3号（`E_502_CARRIAGE03`）。判定前先置 `ev502_return_rolled`，三条出口都算用掉这一次机会；此后重复调查一律落到 `E_502_LOCKED`，只播「门被关死，打不开。」并留在空车厢。到过伪4（`inner_world_entered`）后该门解锁，改为磨损门描写 `E_523` → `E_524` → 真实2号车厢。 |
| 花草左门 | 不设防：无论正程/回程都是「你向来路折返。」→ 切回空车厢（`E_503_BACK` → `E_502`），不再有 50/50 静默随机。 |
| 伪4号左门 | 一律回到花草车厢（`E_522`）。 |
| 花海调头 E_513 | 先播离开，切伪4号，播放抵达后停下。 |
| 伪4号窗边交谈 E_516→E_520 | 谈话结束不再弹「原路返回／继续深入」选择框（与左右门热点重复），也不再写任何回程旗标：点左门＝回花草车厢（`E_522`），点右门仍进花海。 |
| 返程顺序 | 伪4号左门 → 花草车厢 → 花草左门 → 空车厢 → 空车厢左门（磨损门）→ 真实2号车厢（`E_524`）→ 有需要才归还钥匙。 |

描写去重：`E_501` 入口描写、`E_502` 空车厢描写、`E_503` 花草车厢初见描写、`E_522` 花草车厢返程描写、`E_523` 磨损门描写各自只播一次，分别由 `ev502_intro_seen` / `ev503_intro_seen` / `ev522_intro_seen` / `ev523_seen` 记录；重复进入时仍保留「你穿过车门。」「你向来路折返。」这类动作句，只跳过车厢与门的描写。瓶子相关文字按约定不去重：未拾瓶时 `E_503_TAIL` 仍落到 `E_503_BOTTLES` 播瓶堆提示，持瓶时 `E_522_TAIL` 仍落到 `E_522_BOTTLE` 播摸瓶描写。瓶子只通过 `E_503_PICK` 点击获得，不自动补拿。持瓶描写按实际背包判断，拾瓶/侦察结束不重播入场。

不再有回程旗标：原 `ev_inner_backtrack`（`E_513` 与 `E_520` 置位、`E_524` 清除、两扇左门读取）已随本次改造删除；派生事件 `E_503_RETURN_FORWARD`、`E_509_BACK` 一并删除。本次新增 `ev502_return_rolled`（判定已用掉）与四个「描写已播」旗标，另新增 `E_502_LOCKED`（门被锁上）、`E_502_REVISIT`（空车厢已看过描写的静默落点）、`E_503_TAIL`、`E_522_TAIL`（瓶子收尾）。

`carriage_06_eaten` 的唯一来源是 E_501 踏入空车厢那一刻（每条进入里世界的路径都必经此处）；返回分支不再参与置位。6号场景的背景变体读这个旗标，指向 `assets/carriage-06-eaten.png`（被啃食·红色），且排在“便签消失版”之前，因此两个条件同时成立时显示被啃食画面。

伪4号背景默认完整雾景，侦察成功者在 `E_517` 的“雾气散开”后设置 `ev517_flower_revealed` 并刷新完整花海背景；重复调查直接独白。该标记与 `ev510_flower_sea` 污染独立。窗户蒙版跟随揭示标记，门的层级高于窗户以避免重叠误触。

## 演出与状态

- `crew_met` 表示交互过，继续沿用主线置位点；不再以未交互代表死亡。保留两条低语分支，修正未交互者认得声音、坐姿与站姿等矛盾。
- 音效制作说明、状态标签、条件指令和数值待定提示不再显示给玩家；已有剧情声音描写保留。
- `innerWhisperScare`：红底黑字“停下来”、600字/秒、剧烈震动、5秒有效播放时间，不加音效、不扣SAN。暂停冻结，点击/快进不缩短，取消清理覆盖层。
- `ev519_key_given`：钥匙尚未归还；交出时 `removeItem`，出口 `addItem` 后清除。`ev519_key_ever_given`：永久历史，用于控制杆抓握区20%→15%；不改变20秒时限和远端新增失败路线 `E_030_TUG`。
- 终局接线补齐：主游戏加载 `conductor-tug.js`，`E_028` 从旧检定改为实际争夺小游戏；自然失败或退出均经 `E_030_TUG` 收尾。四张正式素材复制到 `assets/ui/conductor-tug/`，独立预览仍可使用源素材。
- `lost` / `trauma` 接入结束页；Trauma仍是涉足花海后的原真结局替代。原真结局、失败结局保留已有结尾视频。
- 存档版本保持3。缺省新旗标视为未触发；旧档尚未归还钥匙可在出口收尾，已清除的交钥匙历史不猜测恢复。已删除事件不是存档恢复执行入口，不恢复半段事件栈。旧档中途回程会按新规则继续（花草→空车厢→磨损门→2号），不会出现死路；可感差异是旧的 `ev_inner_backtrack: true` 不再产生效果、已看过的描写可能再播一次，且已用过左门判定但没有 `ev502_return_rolled` 的旧档会多得到一次判定机会。

## 明确暂缓

1. TODO：恢复鬼打墙演出；回程现在由「花草车厢 → 空车厢 → 磨损门」逐门承接，仍无鬼打墙旁白（原 `E_521` 占位已随窗边弹窗一并删除）。
2. 磨损车门缺素材：保留剧情描写，暂用空车厢左门的隐形热点承担（该热点的悬停/无障碍名仍写作「来路的车门」）。

第2项是审阅者明确接受的文本/画面例外。6号被啃食已落地：`Assets/Image/Scene/Background/里6.png` 复制为 `assets/carriage-06-eaten.png` 并接入 `carriage_06.backgroundVariants`。Assets 源素材不修改；已复制完整伪4号雾景到游戏运行资产。

## 验证

`npm test` 包含现有运行时测试和 `tools/test-inner-world.mjs`：逐句场景轨迹、门禁两拍（进空车厢只带啃食标签且可再进、到伪4才锁门禁）、空车厢左门首次判定三档且只掷一次随机、判定用掉后被锁上并留在空车厢、到过伪4解锁后进真实2号、花草左门两种随机值都不设防、五段描写各只播一次而瓶子两段仍重播、返程逐门链路（伪4→花草→空车厢→磨损门→2号）、回程再前进、交互/侦察分支、手动拾瓶、钥匙交出/归还/重复操作、迷失/Trauma/真结局、暂停中等待切景、慢加载取消与加载失败回滚；并断言 3号车门指向 `E_501`、且 `E_501` 保留“到过伪4后走 `E_DOOR_03`”的守卫。

浏览器验收另检查真实素材、门窗交叠、5秒演出及暂停取消、背包显示和终局抓握区；本次新增的“进里世界后回6号显示被啃食画面（与便签消失版同时成立时啃食优先）”同样需要在浏览器里确认。接口变更详见 [接口说明](API使用说明.md)。

本次改造后的浏览器验收路径：3号推门 → 空车厢（初见描写）→ 空车厢左门连点两次（第二次只报「门被关死，打不开。」）→ 花草车厢（初见描写，重复往返不重播）→ 伪4号 → 左门回花草（返程描写一次）→ 左门回空车厢 → 左门磨损门 → 真实2号；另跑一次 30% 回3号后重进、10% 回6号被啃食画面，以及花海调头与窗边交谈（交/不交钥匙）后的同一条返程链。

上一轮浏览器已验证：真实点击空车厢→花草拾瓶→伪4雾景→窗边揭示→交钥匙→花海调头→逐门返回2号；每句开始时实际 DOM 图片均已就绪。另验证惊吓暂停冻结/取消清理、20%与15%抓握区、迷失/Trauma结束页及存档恢复。浏览器初次发现的 DOM 图片就绪延迟已通过 `whenReady()` 等待修复。

最终计时实测：不暂停约5080ms，暂停约900ms后总时长约5996ms；原真结局与小游戏退出后的失败结局均继续进入原结尾视频。`npm run check` 包含编译、语法检查和两组回归；测试输出中的故意错误用于验证失败回滚，不代表测试失败。
