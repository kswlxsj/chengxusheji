# 浏览器兼容性调查与改造计划

> 调查日期：2026-09-21  
> 当前阶段：仅完成调查与计划，尚未执行兼容性改造。

## 1. 目标与已确认口径

- 支持 Chrome、Edge、Firefox 当前稳定版及之前两个大版本。
- “IE 兼容”指 IE11 的 Trident 引擎，以 Microsoft Edge 的 IE 模式作为正式验收环境；不支持 IE10 及更早版本，也不要求已经退役的独立 IE 桌面程序。
- 范围包含 `Game/` 下全部玩家页面，包括登录、注册、主页、设置、存档、游戏、结局页面以及完整的 `Game/GroupIntro/` 子站。
- Chrome、Edge、Firefox 必须保持当前内容、交互、时序和表现效果。
- IE11 必须保证流程、剧情、操作、小游戏、存档和结局完整；允许字体栅格、模糊、混合模式、复杂渐变等纯表现效果适当降级。
- 尽量隔离 IE 兼容实现，避免大范围重写现代源码。

Microsoft 已于 2022 年结束部分 Windows 10 上 IE11 桌面应用的支持，并建议使用 Edge IE 模式；IE 模式承诺至少支持至 2029 年：

- <https://learn.microsoft.com/en-us/lifecycle/products/internet-explorer-11>
- <https://learn.microsoft.com/en-ie/microsoft-edge/devtools-guide-chromium/ie-mode/>

## 2. 项目现状

项目是无前端框架、无玩家侧构建步骤的原生 HTML/CSS/JavaScript 多页面游戏。运行数据由 `data/*.json` 编译为 `data/compiled-game-data.js`，浏览器通过传统 `<script>` 标签按顺序加载运行时模块。

调查时仓库位于 `main`，本地与 `origin/main` 一致，工作区干净。项目现有测试不全绿：`npm test` 的运行时和里世界测试通过，但 `tools/test-main-route.mjs` 对 `black_bag_03.visibleWhen` 的断言落后于当前 `scenes.json`，因此在主线测试处失败。这是调查前已存在的测试基线问题，不是浏览器兼容问题。

## 3. 实测结果

### 3.1 Chrome、Edge、Firefox

实际测试环境：

| 浏览器 | 版本 | 结果 |
| --- | --- | --- |
| Google Chrome | 153.0.8010.48 | 登录入口、存档槽和游戏属性分配界面正常，无控制台错误 |
| Microsoft Edge | 153.0.4234.48 | 登录入口、存档槽和游戏属性分配界面正常，无控制台错误 |
| Firefox | 156.0 | 完成注册、登录、主页 OP、新游戏槽位、属性分配和开场对白，无控制台错误 |

三者均确认支持项目使用的 WebP、H.264 MP4 和 MP3。当前主流程在现代浏览器中没有发现入口级兼容故障，但这次实测不是完整通关测试，仍需在实施阶段覆盖小游戏、设置、读写档、全部结局和成员介绍页。

### 3.2 现有现代浏览器缺陷：感谢视频

`Game/assets/Video/thanks.mp4` 的视频编码是 HEVC/H.265（`hvc1`）。上述 Chrome、Edge、Firefox 测试环境均通过 `HTMLMediaElement.canPlayType()` 返回不支持。HEVC 是否可播放取决于操作系统、硬件和额外解码组件，不能作为跨浏览器必备能力。

此外，相关代码使用了大小写错误的 `assets/video/thanks.mp4`，而实际目录是 `assets/Video/`；在 Windows 上可能被掩盖，在区分大小写的服务器上会直接 404。`trueend.mp4` 也存在相同目录大小写写法。

Microsoft 建议在需要广泛兼容时使用 H.264 视频和 AAC 音频的 MP4：

- <https://learn.microsoft.com/en-us/troubleshoot/microsoft-edge/development/video-playback-issues>
- <https://developer.mozilla.org/en-US/docs/Web/Media/Guides/Formats/Video_codecs>

### 3.3 IE11/Trident 当前完全不可运行

IE 问题不是单点缺陷，而是语法、运行时 API、样式和素材四层同时不兼容。

#### JavaScript 语法

- `Game/src/` 共 37 个 JavaScript 文件，37 个都包含 IE11 无法解析的现代语法。
- 其中 25 个使用可选链 `?.` 或空值合并 `??`。
- 还广泛使用 `const`、`let`、箭头函数、类、模板字符串、解构、展开语法、默认参数、`for...of`、`async/await` 和对象方法简写。
- `Game/GroupIntro/dxh/vendor/marked/marked.umd.js` 为 Marked 18.0.7，同样包含类字段、可选链、对象展开、Unicode 正则等 IE11 无法解析的语法。

IE 会在解析首批脚本时直接抛出语法错误，登录、注册和游戏逻辑都不会启动。可选链在主流浏览器中直到 2020 年才普遍可用：

- <https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Optional_chaining>

#### JavaScript 与 DOM API

源码使用了 IE11 原生缺失或支持不完整的能力，包括：

- `Promise`、`Map`、`Set`、`URL`、`URLSearchParams`；
- `Object.entries()`、`Object.fromEntries()`、`Array.from()`、`includes()` 等；
- `Element.closest()`、`Element.remove()`、`append()`、NodeList 遍历和事件监听器 `{ once: true }` 选项；
- `IntersectionObserver`、`ResizeObserver`；
- Worker 内的 `fetch()`、`createImageBitmap()`、`OffscreenCanvas`。

透明像素命中已经有 Worker 失败后回退主线程的设计，可以在 IE 中直接停用 Worker 并保留主线程路径。WebGL 演示也已有“不支持则跳过”的分支。

#### CSS 与布局

主样式和成员介绍页大量使用：

- CSS 自定义属性和 `var()`；
- CSS Grid、Flex `gap`；
- `aspect-ratio`、`object-fit`、`clamp()`、`min()`、`max()`、`inset`；
- `backdrop-filter`、`mix-blend-mode`、`clip-path`、`accent-color`、`focus-visible`；
- 由 JavaScript 动态写入 CSS 变量驱动的控制杆、收音机、骰子和结局动画。

IE11 不解析 CSS 变量，许多颜色、边框和动态 transform 会整体失效；Grid、比例布局和图片适配也会造成明显错位。仅做 JavaScript 转译不能解决这些问题。

#### 图片、字体与媒体

- `Game/assets/` 与 `Game/GroupIntro/` 共存在 144 个 WebP，约 69.17 MiB；场景背景、人物、物件、HUD、骰子和成员页都在使用。
- IE11 不支持 WebP，当前页面会大面积缺图。MDN 的兼容格式清单只将 WebP列为 Chrome、Edge、Firefox、Opera 和 Safari 支持，而 PNG/JPEG 包含 IE：<https://developer.mozilla.org/en-US/docs/Web/Media/Guides/Formats/Image_types>
- 项目唯一字体资源为 WOFF2，IE 环境需要允许系统字体回退，或另行生成 WOFF 版本。
- MP3、H.264/AAC MP4 可作为 IE11 的兼容媒体基础；HEVC 感谢视频必须提供 H.264 后备源。

### 3.4 IE 实测环境限制

当前 Windows 环境中的独立 IE 自动化对象已不可用，符合微软退役独立 IE 的现状。Edge IE 模式不能通过普通 Playwright 会话自动化；最终必须在启用了 IE 模式的 Edge 中进行人工验收，并可使用微软的 IEChooser 调试 Trident 页面。

## 4. 统一改造计划

### 4.1 建立隔离的 IE11 构建

1. 保留现有现代脚本和加载顺序，不把主源码手工降级为 ES5。
2. 增加兼容构建脚本，将主游戏、小游戏、结局演出、成员介绍脚本和 Marked 转译为 IE11 可解析的 ES5 镜像。
3. 在每个页面为对应脚本增加 `nomodule` 兼容版本；现代 Chrome、Edge、Firefox 跳过兼容脚本并继续执行现有源码，IE11 因不识别 `nomodule` 而加载 ES5 版本。
4. 把现有 HTML 内联脚本移到可独立转译的外部文件，保持执行顺序和行为不变。
5. IE 脚本链最先加载 polyfill，补齐 ES 内建对象、URL、DOM 操作、事件监听选项和 IntersectionObserver 等实际使用能力。
6. 使用现有 `pnpm-lock.yaml` 锁定 Babel、core-js、PostCSS、Autoprefixer、ES5 校验器和素材转换工具；玩家交付物继续包含生成结果，不要求玩家安装 Node.js。

### 4.2 IE11 专用样式与交互回退

1. 从现有样式生成 IE 专用 CSS：展开静态 CSS 变量、添加 IE 前缀，并以人工覆盖补齐无法自动转换的布局。
2. 对 Grid、Flex `gap`、`aspect-ratio` 和 `object-fit` 使用固定尺寸、Flex/块布局、伪元素比例盒或绝对定位回退。
3. 对控制杆、收音机指针等影响玩法的动态 CSS 变量，在 IE 分支直接写入 `transform` 等真实属性，保证判定反馈可见。
4. 对 `backdrop-filter`、混合模式和复杂渐变采用纯色、半透明背景或简化阴影；这些降级只在 IE 样式中生效。
5. 成员介绍页缺少 IntersectionObserver 时直接显示内容或使用轻量滚动回退，不能因装饰动画缺失而隐藏正文。

### 4.3 图片与媒体回退

1. 构建脚本扫描实际引用的 WebP：带透明通道的图片生成 PNG，完全不透明的图片生成高质量 JPEG，并产生确定性的路径映射清单。
2. 新增 `TrainGame.Compat.assetUrl(path)`：现代浏览器原样返回路径，IE11 返回兼容图片路径。场景、UI、物品、结局演出和背景音画模块统一在设置资源 URL 时调用该接口。
3. 主样式中的 HUD、背包框、对话框和怪物图，以及成员介绍页的静态 WebP，由 IE 专用 CSS/适配脚本替换。
4. 兼容素材按 `Assets/` 与 `Game/assets/` 同路径、同字节规则同步，并在素材规范中登记其“构建生成的 IE 后备资产”身份。
5. 保留原始 HEVC `thanks.mp4`，新增 H.264/AAC 后备版本；使用多个 `<source>` 让支持 HEVC 的环境保持原视频，不支持的浏览器自动选择 H.264。
6. 统一修正 `assets/Video/` 的路径大小写，并增加大小写敏感服务器检查。

### 4.4 构建接口与文档

在 `package.json` 中增加：

- `compat:build`：生成 ES5 脚本、IE CSS、兼容素材和映射清单；
- `compat:check`：校验 ES5 语法、资源完整性、路径大小写和生成结果是否最新；
- 将兼容检查纳入现有 `check`。

文档调整：

- 新增细分浏览器兼容说明，记录支持矩阵、IE 模式开启方法、允许降级项、构建命令和验收流程；
- 更新 `Game/README.md`、`Game/启动说明.md`、`Game/docs/README.md` 和 `Assets/README.md`；
- 明确数据格式和存档版本不变，不修改剧情、事件接线、数值或存档协议。

### 4.5 处理既有测试基线

先把 `tools/test-main-route.mjs` 中过时的 `black_bag_03.visibleWhen` 断言更新为当前数据结构，使测试反映既有游戏行为。只调整测试期望，不修改 `scenes.json` 或剧情效果，并在兼容提交中明确这是调查前已存在的基线修正。

## 5. 测试与验收计划

### 自动检查

- 兼容构建连续运行两次，第二次不得产生差异。
- 所有 IE 脚本通过 ES5 解析检查，不得残留可选链、类、箭头函数、模板字符串、异步函数或现代正则语法。
- 每个原始 WebP 引用在 IE 映射中都有存在且非空的后备文件。
- 校验 HTML、CSS、JSON 和 JavaScript 中的资源路径大小写。
- 运行 `npm run check`、`git diff --check` 和资源同步校验。

### Chrome、Edge、Firefox

- 当前稳定版执行完整浏览器回归；前两个大版本执行相同 smoke matrix 或使用版本化运行环境验证。
- 覆盖注册、登录、主页 OP、设置、三个存档槽、新游戏、暂停、保存、读取、刷新恢复、全部小游戏、全部结局和成员介绍页。
- 验证感谢视频在不支持 HEVC 的环境自动使用 H.264。
- 对关键页面和演出保存改造前后同尺寸截图，确认现代浏览器布局和表现没有变化。

### Edge IE 模式

- 从 `index.html` 正常注册、登录并进入游戏。
- 验证主页 OP、属性分配、对话推进、检定、场景热点、透明像素命中、物品快捷栏、暂停和三个存档槽。
- 分别验证卡牌、交涉、收音机和控制杆争夺；WebGL 隐藏演示允许走现有“不支持则跳过”分支。
- 验证全部结局、H.264 感谢视频、设置页和六个成员介绍页。
- 验收硬标准是无语法错误、无流程死锁、无关键图片/文字缺失、所有操作可完成；已确认的纯装饰降级不作为失败。

## 6. 明确不做的内容

- 不支持 IE10 及更早版本。
- 不恢复或分发独立 IE11 桌面应用。
- 不改变剧情文案、事件顺序、检定规则、存档结构或游戏数值。
- 不为追求 IE 像素级一致而重写整个现代前端；现代浏览器继续使用现有实现。
- 本文落地时不执行上述改造，后续实施需以本文为审查基线。
