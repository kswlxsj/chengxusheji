# 主线场景接线维护记录

本次按审阅者确认的统一修复计划执行。源数据为 `data/scenes.json` 与 `data/events.json`，引擎为 `src/events.js`；回归见 `tools/test-main-route.mjs`。本文件描述当前行为与接线约定。

## 接线约定

1. **先切景，再播到达描写**：任何进入新场景的路线都必须先执行 `changeScene`，目的地描写（“一进入车厢……”“你踏进2号车厢。”）必须落在新背景上。
2. **出发文字留在出发场景**：推门、过门、起步这类句子属于出发场景，不因为下一句要切景而提前切。
3. **点击目标必须语义匹配**：场景热点的 `clickEvent` 指向的事件，其内容与状态修改必须属于该场景；门类热点优先复用 `E_GO_<出发>_<到达>` 命名。
4. **同一段落只播一次**：进车厢类检定用独立旗标闸门；到过一个场景的记录（如 `crew_met`）不因路线不同而缺失。
5. **进车先点门，剧情不许代劳**：从一节车厢进入下一节车厢必须由玩家点击门热点（`door_<出发>_<到达>`）触发；剧情事件不得用 `changeScene` 直接替玩家完成跨车厢。否则玩家没有机会在门前做选择——3号→2号的门同时是里世界入口，一旦由剧情自动进车，一条主要支线就会被整段跳过。
6. **一次性演出使用用途单一的旗标**：首次发现、长演出、惊吓、长独白和首次 SAN 在事件首部读取专用旗标，只播放一次；重访落到静默事件或短反馈。进度门槛与演出已读状态分离，例如 `ev023_intro_seen` 只管门前认知崩塌，`inner_world_entered` 继续决定3号门通往里世界还是真2号。

## 修复对照（第一轮：统一接线）

| # | 原现象 | 现在的行为 |
| --- | --- | --- |
| 1 | 5号右门 `door_05_to_04` 直连 `E_013`，无切景、无到达描写，直接弹医学检定 | 右门改指新增 `E_GO_05_04`（`changeScene carriage_04` + 置 `crew_met` +「你穿过门，来到4号车厢。」），**不触发检定** |
| 2 | 剧情路线（`E_010_F`／`E_010_JOIN`／`E_011_S`／`E_011_F`／`E_012_AFTER`）直接落进只有检定动作的 `E_013` | 五条路线统一 `next → E_013_ENTRY`：首次到达只切到 4 号车厢并播一遍发现乘务员的描写，不再自动检定 |
| 3 | `E_012_AFTER` 在5号背景上说「你来到4号车厢。」 | 该句收缩为「本能驱使着你向前跑去。」，到达描写归 `E_013_ENTRY` |
| 4 | 首次发现乘务员的描写与检定每次进入4号都会重播（6号↔7号门往返会重走 `E_010_F`） | `E_013_ENTRY` 增加 `crew_04_entry_seen` 门禁（兼容旧档的 `crew_04_entry_medical_done`）：重复到达落到短反馈 `E_013_REVISIT`，发现描写与 `crew_met` 只在首次置位 |
| 5 | 点击乘务员后的处理仍依赖单独技能状态 | 乘务员热点 `E_013` 不再弹选择，直接执行教育检定。同一处理最多检定两次，第一次成功立即结束，第一次失败可再试，第二次无论成败都结束 |
| 6 | `E_DOOR_04`（4号→3号）在行李已清理后静默切景 | 末尾补「你穿过门，返回3号车厢。」，与 `E_GO_02_03` 同形 |
| 7 | 3号→2号（`E_023_LOOP`→`E_024`→`E_025`→`E_026`）全程停在3号背景 | `E_024_S_CONTINUE` 首部、`E_024_F` 末尾各补 `changeScene carriage_02`；`E_025` 收敛点因此场景中立（**第二轮已作废**：改为由玩家点门进车，`E_024` 随之退出主线，见下） |
| 8 | `clicker_02` 指向 `E_021`（内容是「回到3号车厢，你取出工具。」） | 改指 `E_026`，直接进入2号车厢的怪物遭遇 |
| 9 | `control_27`（前进与停车把手）指向 `E_027`（2号车厢体质检定） | 改指 `E_032`（打开操作面板） |
| 10 | `E_027_S` 通过不置 `carriage_02_passed`，前门此后永远只能「强行推门」 | `E_027_S` 补置 `carriage_02_passed = true` |
| 11 | `E_GO_02_FRONT_DOOR` 的「推开安全门」指向 `E_026`（怪物遭遇） | 改指 `E_031`（到达先头车厢） |

新增内容：事件 `E_GO_05_04`、`E_013_REVISIT`、`E_013_USE`、`E_013_USE_SECOND`、`E_013_CANCEL`、`E_013_F_RETRY`；旗标 `crew_04_entry_seen`、`crew_04_medical_attempted`；共享检定 `checkId: crew_04_medical`；回归脚本 `tools/test-main-route.mjs`。存档版本（3）与数据格式版本（3）不变；新旗标缺省视为未触发。已持有 `crew_04_entry_medical_done` 的旧档会按“已看过首次发现演出”处理。

> 注：本轮表格中的 `E_010_F`／`E_010_JOIN`／`E_011_F` 已在后续「清理批次」中作为不可达事件删除，`E_011_S`／`E_012_AFTER` 仍是主线可达路线；本条记录保留历史原貌。

## 修复对照（第二轮：2号车厢改由玩家点门进入）

审阅者反馈：3号→2号的剧情链会自动把玩家带进2号车厢并一路推进，玩家点不到那扇门，里世界支线形同虚设。

| # | 原现象 | 现在的行为 |
| --- | --- | --- |
| 12 | 剧情链 `E_022_ITEM → E_023 → E_023_LOOP → E_024` 全自动串联，最后 `E_024_S_CONTINUE`／`E_024_F` 各自 `changeScene carriage_02` 并接 `E_025 → E_026`，把玩家直接送进2号 | `E_022_ITEM` 拿完手电即停（不再自动接 `E_023`），玩家留在3号车厢自己决定走哪扇门；进2号只能点 `door_03_to_02`（`E_501`），首次进里世界、已进过则走主线 `E_DOOR_03`（由 `845babf` 落地） |
| 13 | 进2号只剩自动路径，`E_025`（进入2号·听到喘息，含乘务员同行分支 `E_025_CARRIED`）成为主线进车描写，里世界出口无人接管 | `E_025` 改由里世界返程承接：`E_524` 回到真2号后经 `E_524_DONE`（乘务员同行时先经 `E_524_CREW`）进入 `E_025`；`E_025` 与 `E_025_CARRIED` 的 `next` 均已删除，喘息段播完停下，玩家照明后自己点 `clicker_02` 触发 `E_026` |
| 14 | `E_524` 与 `E_025` 各写了一遍喘息（「你听见明显的喘息声。」「那不是人类的喘息。」） | 喘息描写只在 `E_025` 保留一次；`E_524` 的到达句收缩为「这一次，门后是真正的2号车厢。」 |

本轮无新增事件、无新增旗标；存档版本（3）与数据格式版本（3）不变。已进过里世界但在旧档里停在2号的存档，下次离开里世界或点门时按新规则继续，不产生死路。

> 注：本轮表格中的 `E_024`／`E_024_S_CONTINUE`／`E_024_F`（光源侦查旧线）已在后续「接线批次」删除；该轮「进2号只能点门」的结论不变，点门现在先播 `E_023` 门前认知崩塌再交 `E_501`。

## 修复对照（第三轮：瓶子只能从里世界获取）

内容侧决定：瓶子（`bottle`，界面名「彩色玻璃瓶」）只能从里世界花草车厢拾取（`E_503`／`E_522`），2号车厢不再就地补发。

| # | 原现象 | 现在的行为 |
| --- | --- | --- |
| 15 | `E_028` 在玩家没有瓶子时先播「你在脚边摸到一个空瓶子，把它捡了起来。」并 `addItem`，等于2号车厢自供瓶子，「只能从里世界获取」形同虚设 | 删除就地拾瓶分支与 `E_028_HAS_BOTTLE`；`E_028` 收窄为保留投掷状态 +「你记住了投掷的手法。」随后接 `E_028_BOTTLE_READY` |
| 16 | `E_026`／`E_026_KNOWLEDGE`／`E_027_F` 的「捡起手边的东西（瓶子）制造声响」选项无条件出现，没瓶子的玩家照样能掷瓶 | 三个选项统一加 `when: { hasItem: "bottle" }`，文案改为「投掷彩色玻璃瓶…」；没有瓶子时该选项被引擎过滤，只剩安静通过／正面对抗，不产生死路（`E_029` 正面对抗始终可选） |

`E_028_HAS_BOTTLE` 已被删除（唯一引用方是 `E_028`），不留死事件。后续 JSON 剧情细化批次确认 `E_028` 整条体质/幸运链也无入口，因此继续删除该链，只保留背包主动使用所需的 `E_028_THROW_FIRST`。存档版本（3）与数据格式版本（3）不变——旧档里已持有的瓶子继续生效，不做回收。

## 乘务员死亡线工具与线索边界（2026-09-17）

- 4号车厢保留员工柜前的第三次医学检定；若失败，置 `crew_04_dead`，乘务员不再同行或提供口述情报。
- 死亡不会锁住员工柜：`E_020_DEAD_TOOLS` 仍发放应急割带器与撬杆并置 `tools_ready`，因此可回3号清开行李、打开黑包并继续主线。`E_020` 对已死亡但尚未取得工具的旧存档同样转入该分支。
- 独自路线只能依据玩家亲眼发现的黑包与手机未来短信推进；不得引用乘务员未说出的黑包位置、停车要求、员工柜位置或钥匙情报。手机短信的「不要停车」始终独立于乘务员是否存活。

## 手动移动与一次性演出批次（2026-09-15）

本批把主线跨车厢推进统一改为玩家点门或明确选择后发生；剧情事件播放完均回到当前场景的稳定状态，门槛解锁后永久允许双向往返。

| 边界 | 当前门槛与落点 |
| --- | --- |
| 6→7 | 完成6号调查后可点门；灵感成功可选择继续或留下，失败则按现行剧情自动推门进入7号 |
| 7→6→5 | 看完7号深处后停在7号；点返回门进6号只播放回程台词，不再重播空车厢演出，再由玩家点门进5号 |
| 5→4 | 取得报纸后开放；5号背包只取得手电筒，读报与黑暗逼近均停在5号，点门后才切4号并播放一次乘务员初见 |
| 4→3 | 至少处理一次乘务员后开放；背起、留下或救治结果均停在4号，点门才进入3号 |
| 3→4→3 | 发现缺工具后停在3号；返回4号取工具仍停在4号，再点门回3号后继续清行李、开黑包 |
| 3→2 | 黑包、钥匙和手机段完成后开放；首次点门进里世界，完成该段后按既有规则进真2号 |
| 2→先头车厢 | 潜行、投瓶与卡牌胜利均停在2号安全门前；`carriage_02_passed` 为真后由玩家点安全门进入 |

主要去重旗标包括 `carriage_07_entry_seen`、`corpse_07_investigated`、`ev008_scouting_done`、`ev009_seen`、`carriage_05_entry_seen`、`carriage_03_first_entry_seen`、`carriage_03_bag_resolved`、`clicker_first_encounter_seen`、`front_carriage_entry_seen`；里世界旗标见 [里世界演出维护记录](inner-world-presentation.md)。旗标缺省均为 `false`，存档版本和数据格式版本保持 3；兼容旧档时也读取已存在的阶段性旗标，避免已完成流程被误判为未解锁或重播。

### 3号车厢行李演出（2026-09-16）

3号背景按 `carriage_03_bag_interacted`、`carriage_03_bag_exposed`、`carriage_03_bag_resolved` 依次表现满载、半清理、只剩黑包与空车厢。旗标改变后立即执行 `refreshScene`，确保行李塌落和黑包调查发生在对应画面上；`carriage_03_bag_resolved` 优先级最高，因此旧档即使没有新增的 `carriage_03_bag_exposed` 也会直接显示完成状态。包内 `MOVE FORWARD` 纸条只在相关对白期间由 `carriage_03_forward_note_visible` 短暂显示，不作为持久场景热点。

5号工具背包已改用独立的 `carriage-05-03-clutter.png`，避免3号新版黑包移位影响5号热点；7号尸体与收音机仅更新同坐标新版素材，剧情接线不变。4号、6号素材经审计与源素材一致，无需调整。

文案同步口径：开场日期为 2013年7月15日；乘务员关于钥匙的问答只保留钥匙位置、取工具与停车操作；右杆统一为「上拉持续减速直至停车」；死亡线声音既像乘务员又陌生。未来日期、幸存人数变化、乘务员记忆错乱及「停车／继续前进」对立仍是刻意悬疑，不作矛盾修正。

## JSON 剧情与检定细化批次（2026-09-16）

本批只以 `data/*.json` 与现行运行时代码为准，不再从旧 Markdown 剧本回填内容。剧情走向不变，重点补足检定前的可执行动作、成败反馈与跨事件因果。

| 项目 | 当前规则 |
| --- | --- |
| SAN 路由 | `sanCheck` 只负责判定与扣损，返回 `0=成功 / 1=失败`；剧情结果由事件接线承接。SAN 在检定中归零时，引擎先终止，不播放后续事件。Clicker 初见不再扣 SAN，精神损耗移到潜行三段。 |
| 7号侦察 | `E_008` 新增 `ev008_insight_01`。成功看清黑暗中的嘴并承受 SAN，失败只确认热风、挤压声与未知危险；两路经 `E_008_AFTER` 汇合。 |
| 交涉 | 三轮每答对一题增加 15%，最终成功率为 40% / 55% / 70% / 85%；检定窗口只显示概率与骰点，剧情结果留在 `E_014_TALK_S/F`。 |
| 3号综合检定 | `ev021_education_insight_01` 取教育与灵感的向下取整平均值，使用 `2d6 + 平均值 ≥ 15`；双6／双1仍按大成功／大失败处理。 |
| 2号潜行 | 连续三轮标准 `2d6 + 体质 ≥ 14`；每轮体质成功后做一次单颗 d6 SAN 检定，4—6 不扣、1—3 扣 1。任一体质失败进入困难卡牌，三轮全部完成才清除 Clicker。 |
| 2号投瓶/迎战 | 持瓶初见只提示「你摸了摸口袋里的瓶子」，玩家从物品栏触发隐藏路线。投瓶用 `2d6 + floor((体质 + min(SAN, 10)) / 2) ≥ 12`，8/8 时失败率约 8.3%；失手消耗瓶子并进困难卡牌。正面迎战不再前置体质检定，直接进简单卡牌。 |
| 冗余清理 | 删除无入口的 `E_028` 旧体质/幸运链，背包入口指向现行 `E_028_THROW_FIRST`；删除空转事件与无读取方旗标写入。`throwing` 技能定义仅为旧存档键集合兼容而保留。 |

本批新增或恢复玩家可见的重访短反馈，不改变阻止重复领物、重复演出的内部终点。存档版本与数据格式版本仍为 3。

## 六面骰数值重平衡批次（2026-09-16）

属性创建总值固定为32，默认均衡分配为体质／教育／灵感／SAN各8；普通属性创建下限3、运行时下限1、上限10，SAN创建下限1、运行时下限0且不设上限。普通检定统一为 `2d6 + 属性 ≥ 14`，教育＋灵感综合检定为 `2d6 + 向下取整平均值 ≥ 15`；双6为必定成功的大成功，双1为必定失败的大失败。骰窗并排复用两张六面骰素材，大成功显示金黄色、大失败显示稍暗红色，音效复用普通成功／失败。

SAN检定不再读取当前SAN：通用检定固定单颗d6，4—6成功、1—3失败；初见被啃食的6号按1／2／3分别扣3／2／1，4—6不扣；仅惊吓线 `E_FAKE01_EXIT` 在「你失去了呼喊的力气。」后退出里世界时按1扣2、2—4扣1、5—6不扣。7号实际呕吐分支扣体质1。5号两件行李、工具背包、报纸和两件杂物拆为六个首次调查点，全部完成后只奖励一次灵感1。所有剧情属性变化共用运行时入口，并通过顶部居中的黑色 Toast 显示实际前后值；同一事件连续变化时按发生顺序逐条显示，到达上下限而未变化时显示截断原因。右上角获得物品卡片仅用于物品。

本批将存档版本与数据格式版本提升为4；旧存档保留但标记为不兼容，不自动迁移。

## Clicker 三路线重构（2026-09-16）

- `E_026` 只负责初见演出；不再调用 `ev026_san_01`，持瓶时以模糊动作句暗示物品栏交互。
- `E_027` 拆成三段独立体质检定与三次 0/1 SAN 损失，任一体质失败统一进入 `card_battle_hard`。
- `E_028_THROW_FIRST` 从物品栏触发，先移除瓶子再执行体质+SAN综合检定；成功引开 Clicker，失败在脚边碎裂并进入困难卡牌。
- `E_029` 取消 `ev029_constitution_01` 分流，正面迎战固定进入 `card_battle` 简单模式。数据格式与存档版本保持4。

## 验证

`npm run check` 包含既有三组回归与 `tools/test-main-route.mjs`。主线回归同时锁定结构接线、逐句场景轨迹，以及 Clicker 的三轮潜行、隐藏投瓶综合检定和正面迎战难度；运行时回归穷举关键骰点并验证 SAN 封顶参与投瓶计算。里世界回归继续锁定门前认知崩塌、黑场、返程、瓶子来源与结局接线。

浏览器验收 Clicker 时需分别覆盖：三轮潜行全部成功与任一轮失败；正面对抗直接显示简单卡牌；持瓶初见只出现模糊提示，物品格仍显示普通「点击使用/调查」，点击后成功可通行、失手进入困难卡牌且两路都消耗瓶子。通过后均停在安全门前，由玩家点门进入先头车厢。

## 清理批次：删除不可达事件与死检定（2026-09-15）

依据：对 `data/events.json`（当时 245 个事件）做一次完整可达性分析。边类型覆盖 `next`、`choice.options[].next`、`check.outcomes[]`、`conditionalJump.next`、`jump.next`、小游戏结算跳转（`src/minigame-games/*.js`）、`src/main.js` 的硬编码触发（`E_009`／`E_005_GUIDE`）、`custom.params.lockedEvent`、`scenes[].objects[].clickEvent` 与 `items[].inspectEvent`；起点为 `meta.startEvent`（`E_001`）。条件按「旗标无人写 true / 物品无人发放 / 技能未授予 ⇒ 永假」判定（语义同 `src/scene.js` 的 `evaluateCondition`），事件、场景、物品三者做定点迭代。结果：**可达 198 / 不可达 47 / 悬挂引用 0**，15 个场景全部可达。旧死事件清单只统计 `next` 与选项跳转，未覆盖 `check.outcomes`、`clickEvent`、`inspectEvent` 与小游戏结算跳转，因此漏报了一半以上。

删除清单（24 个事件 + 5 条检定注册 + 1 个不可获得物品）：

| 组 | 删除对象 | 删除理由 |
| --- | --- | --- |
| B | `E_008_F`、`E_009_S`、`E_009_F`、`E_010_S`、`E_010_F`、`E_010_JOIN`、`E_011_F`、`E_021_ALONE_F`、`E_031_NO_KEY_F` | 旧检定的成败分支：这些检定动作的 `outcomes` 已被移除（现在只做副作用），分支的另一半永久失去入口 |
| C | `E_025_SINGLE`、`E_025_MULTI`、`E_025_ESCAPE`、`E_026_FRONT_LEGACY`、`E_030_TUG`、`E_033_S`、`E_033_F`、`E_036` | 先头车厢／控制杆旧段：已被 `E_031`、`conductor_tug` 小游戏结算（`E_034`／`E_035`）与 `endGame` 结局系统取代 |
| D | `E_018_CLEAR_PATH`、`E_05_SEARCH_NEWS_CANCEL`、`E_05_TOOLS_FAIL` | 5 号车厢旧搜索分支，被现行 `E_010`／`E_019`／`E_020` 流程取代 |
| F | `E_901`、`E_902`、`E_903`、`E_904` + 物品 `old_ticket` | 框架演示／引导残留块（`E_904` 自述“由通用窗口基类派生的调查窗口”）；`old_ticket` 只由 `E_902` 发放、只由 `E_903` 调查，整块一并删除 |
| 死检定 | `ev010_join_route_01`、`ev021_san_01`、`ev021_extra_san_01`、`ev030_san_01`，以及删除 C 组后失去唯一引用方的 `ev025_constitution_01` | `src/dice.js` 已注册但没有任何 `check.dice` 引用；`ev030_san_01` 的结局 SAN 检定在 `d68228a` 已被 `custom endGame` 取代 |

同步改动：

- `src/dice.js`：删除上述 5 条注册，并删掉因此不再有调用者的 `endingSanCheck` 辅助函数。后续剧情细化批次又删除了 `ev026_extra_san_01` 与 `conditionalSanCheck`。
- `tools/test-main-route.mjs`：路线断言列表收缩为 `E_011_S`／`E_012_AFTER`；“剧情路线进 4 号”的场景轨迹用例改走 `E_012_AFTER`。
- `tools/test-runtime.mjs`：删除 `ev010_join_route_01` 的分流断言与 `ev030_san_01` 的注册／结局断言；物品调查用例改用 `phone`。
- `data/compiled-game-data.js`：重新生成。
- 文档：`README.md`（`conductor_tug` 条目、编译结果行）、`docs/inner-world-presentation.md`（`E_030_TUG` 两处）、`docs/API使用说明.md`（`E_901`／`E_902`／`E_903`／`old_ticket` 示例改用现存编号与物品）、`skills/script-to-game-data/conversion-rules.md`（保留段示例改为 `E_905`）。

保留未删（23 个）：A 组「旧 2 号车厢穿越线／认知崩塌／光源侦查」20 个、E 组里世界旧分支 2 个（`E_504_F`、`E_515`）、G 组开发演示 1 个（`E_MG3D_DEMO`）。其中 A 组已在后续「接线批次」处置（A2 接回、A1/A3 删除）。

验证：`npm run check` 全绿；`npm run compile` 输出 `15 个场景，221 个事件，8 个物品，4 个属性，1 个技能，6 个小游戏，30 个音效`。

## 接线批次：接回门前认知崩塌、清退 A1/A3 旧线（2026-09-15）

内容侧决定：A 组只接回 A2（门前认知崩塌），A1（旧 2 号车厢穿越线）与 A3（光源侦查旧线）删除，统一采用现行 `E_026`~`E_029` 版本；E 组、G 组不动。

| 项 | 改动 |
| --- | --- |
| 门前接线 | `carriage_03.objects.door_03_to_02.clickEvent`：`E_501` → `E_023`；`E_023_LOOP.next` 仍为 `E_501`，门禁与进里世界/走主线的分工不变 |
| 一次性守卫 | `E_023` 首部新增 `conditionalJump { flag: inner_world_entered == true } → E_501`。语义：**认知崩塌只在「未到过伪4」时播放**，与该门门禁同源——`inner_world_entered` 为真（含读旧档）即跳过演出，直接由 `E_501` 的守卫落到 `E_DOOR_03` 进主线 2 号；「未到伪4就折返回 3 号再推门」仍属首次，会重播（该轮玩家本就尚未真正进入里世界） |
| 3号黑场 | `E_023_LOOP` 末段把「灯，灭了。\n\n黑暗中，你摸到了通往2号车厢的门。」拆为两句，中间插入 `setFlag carriage_03_blackout=true` + `custom refreshScene`；`E_501` 首个动作置 `carriage_03_blackout=false`（离开 3 号即恢复常亮，避免重访时无光源可救） |
| 无光判定 | 新增纯函数 `Game.isSceneUnlit(sceneId, flags)`（`src/scene.js`）：`carriage_02` 看 `light_used`，`carriage_03` 看 `carriage_03_blackout`，其余恒 false；`buildRenderSignature`、`render()` 的 `is-unlit` 与 `is-lit` 统一走它 |
| 变暗演出 | `styles/carriage-02-darkness.css` 复用同一 `::after` 黑场层，只为 `[data-scene-id="carriage_03"]` 加 `scene-blackout 900ms` 淡入；暂停冻结淡入、`prefers-reduced-motion` 下取消。**2 号仍是立即全黑**（不淡入，避免提前看清 Clicker） |
| 删除 A1/A3 | 事件 17 个：`E_02_DECIDE`、`E_022_S`、`E_022_F`、`E_023_CHOICE`、`E_023_CONSTITUTION_CHECK`、`E_023_CONSTITUTION_SUCCESS`、`E_023_CARD_BATTLE_EASY`、`E_023_CARD_BATTLE_HARD`、`E_023_THROW_FIRST`、`E_023_THROW_AFTER_CONSTITUTION_FAIL`、`E_023_THROW_AFTER_CONSTITUTION_FAIL_SUCCESS`、`E_023_THROW_AFTER_CONSTITUTION_FAIL_FAIL`、`E_024`、`E_024_S`、`E_024_S_KNOWLEDGE`、`E_024_S_CONTINUE`、`E_024_F`（与现行 `E_026`~`E_029` 功能一一重复；A1 的掷瓶分支还绕开「瓶子只能从里世界获取」） |
| 删除死检定 | `ev023_constitution_01`、`ev023_throw_after_fail_luck_01`、`ev024_light_01`（唯一引用方即上述被删事件） |

同步改动：`data/compiled-game-data.js` 重新生成；`game.html` 的 `carriage-02-darkness.css`／`scene.js`／`compiled-game-data.js` 缓存戳统一升到 `scene-blackout-20260915-1`；`tools/test-inner-world.mjs`（门目标、守卫与黑场结构、黑场时刻行为、回访跳过、被删对象不得残留）、`tools/test-runtime.mjs`（`Game.isSceneUnlit` 谓词）、`tools/test-main-route.mjs`（门目标与注释）。

验证：`npm run compile` 输出 `15 个场景，204 个事件，8 个物品，4 个属性，1 个技能，6 个小游戏，30 个音效`；`npm run check` 全绿；可达性复跑 **可达 201 / 不可达 3 / 悬挂引用 0**。黑场可观测性由「事件期间锁定场景交互」（`src/events.js` 在 `play()` 起始处 `setInteractionEnabled(false)`）保证：整段 `E_023 → … → E_501` 属同一次 `play()` 链，控制权不会回到 3 号。检查点系统按事件边界记录状态；若手动保存恰好取得黑场后的检查点，其续跑游标会指向 `E_501`，读档后立即清除黑场并继续推进。自动存档仍只在 `!engine.busy` 时触发，不会扩大到链内事件边界。

## 接线批次当时的遗留项（保留历史结论）

1. （已解决）`E_034` 现在读取 `ev510_flower_sea`：未涉足花海进入原 True End，涉足后改走 `E_515` 伪结局（终点站下车后再次看见「MOVE FORWARD」）。
2. （已解决）6号↔7号门仍可往返，但7号深处、返6号乘客消失与相关 SAN 均有一次性守卫，不再因折返重播。
3. （已解决）6号→5号需先完成7号深处并返回；5号→4号需取得报纸，无法再从6号跳过主线线索直达后段。
4. 死事件清单：清理批次删除 24 个、接线批次再删除 17 个之后，仍不可达的只剩 3 个，且都是有意的保留项——E 组里世界旧分支 `E_504_F`、`E_515`（见第 1 项）；G 组开发演示 `E_MG3D_DEMO`（按 `docs/API使用说明.md` 示例九由控制台旗标显式开启，正式剧情不需要时可整体删除）。两批的删除依据、口径与完整清单见「清理批次」「接线批次」两节。（勘误保留：`E_023_BOTTLE` 是物品 `bottle` 的 `inspectEvent`，由物品栏点击触发，属可达事件。）
5. （已在清理批次修复）`README.md` 的「当前开发进度」曾把 `conductor_tug` 的触发点写成 `E_026`／成功接 `E_029`／失败进 `E_030_TUG`；实际为 `E_033` 触发、结算动作跳 `E_034`／`E_035`、退出小游戏不结算。README 与 `docs/inner-world-presentation.md` 的过期说法已改正，死事件 `E_030_TUG` 已随清理批次删除。
6. （已解决）潜行、投瓶与卡牌胜利都会置 `clicker_cleared`，回到2号后不再重复触发遭遇。
7. `crew_04_medical_success` 已在剧情细化批次删除；`crew_04_dead` 不是死旗标——里世界 `E_506` 按它走死亡线（`E_507` 疯狂低语与「停下来」惊吓）与在世线 `E_508`，`E_516` 死亡时走 `E_516_DEAD`。
8. （已在接线批次解决）「认知崩塌」`E_023`／`E_023_PHONE`／`E_023_LOOP` 曾因没有任何热点触发而整段不可达；现由 `door_03_to_02` 触发，`E_023` 首部用 `inner_world_entered` 守卫只在首次播放（见「接线批次」）。「查看2号车厢·光源侦查」`E_024` 全段（含 `ev024_light_01`）按内容侧「用现行版」的决定删除——现行照明由 `useLight` 自定义动作 + `light_used` + `carriage_02` 的 `is-unlit` 全黑实现，知识分支已由 `E_026 → E_026_KNOWLEDGE` 承接 `monster_behavior_known`；A1 的 `E_023_CHOICE` 投掷选项缺 `hasItem: bottle` 门禁的问题随 A1 删除而消失。
9. 3号→4号的门（`door_03_to_04 → E_GO_03_04`）复核结论（接线批次）：黑场期间整段演出在同一次 `play()` 链内、场景交互被锁死，玩家无法点这扇门；`carriage_03_blackout` 在 `E_501` 清除、重访 3 号恢复常亮与门可点，因此**不存在玩法层冲突**，残留的只是重访时的叙事一致性（「来路的门已经消失」这句不会重播）。本轮不改行为；若后续要让该门永久消失，需与 `E_GO_02_03`（2号→3号门）的往返逻辑一并设计。
